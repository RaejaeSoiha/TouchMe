import { api, SubscriptionRow } from '@/lib/api';
export default async function Subscriptions() { const rows = await api<SubscriptionRow[]>('/admin/subscriptions'); return <><h2 className="text-3xl font-black">Subscriptions</h2><div className="mt-8 grid gap-4">{rows.map((row) => <article key={row.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl border bg-white p-5"><div><p className="font-black">{row.user.profile?.displayName ?? row.user.email ?? 'Member'}</p><p className="text-sm text-slate-500">{row.plan.name} · ${(row.plan.priceCents / 100).toFixed(2)}</p></div><div className="text-right"><p className="font-bold">{row.status}</p><p className="text-sm text-slate-500">Renews {new Date(row.currentPeriodEnd).toLocaleDateString()}</p></div></article>)}</div></>; }

