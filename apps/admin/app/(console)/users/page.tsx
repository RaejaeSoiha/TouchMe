import { api, UserRow } from '@/lib/api'; import { UserStatusForm } from '@/components/user-status-form';
export default async function Users() { const users = await api<UserRow[]>('/admin/users'); return <><h2 className="text-3xl font-black">Users</h2><div className="mt-8 overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-100"><tr><th className="p-4">Member</th><th className="p-4">Role</th><th className="p-4">Joined</th><th className="p-4">Verification</th><th className="p-4">Status</th></tr></thead><tbody>{users.map((user) => <tr key={user.id} className="border-t"><td className="p-4"><strong>{user.profile?.displayName ?? 'Incomplete profile'}</strong><br/><span className="text-slate-500">{user.email ?? user.phone}</span></td><td className="p-4">{user.role}</td><td className="p-4">{new Date(user.createdAt).toLocaleDateString()}</td><td className="p-4">{user.profile?.verificationStatus ?? 'UNVERIFIED'}</td><td className="p-4"><UserStatusForm userId={user.id} current={user.status}/></td></tr>)}</tbody></table></div></>; }

